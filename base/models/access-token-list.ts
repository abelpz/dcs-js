import { AccessToken } from "./access-token";

/**
 * AccessTokenList represents a list of API access token.
 * @export
 * @type {AccessToken[]} AccessTokenList
 */

export type AccessTokenList = AccessToken[];
