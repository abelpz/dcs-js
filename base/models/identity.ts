/**
 * Identity for a person&#39;s identity like an author or committer
 * @export
 * @interface Identity
 */

export interface Identity {
  /**
   *
   * @type {string}
   * @memberof Identity
   */
  email: string;
  /**
   *
   * @type {string}
   * @memberof Identity
   */
  name: string;
}
